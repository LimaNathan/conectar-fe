refactor: Melhora a usabilidade da lista de clientes

Este commit introduz as seguintes melhorias na lista de clientes:

- Adiciona um `SingleChildScrollView` para permitir a rolagem da tela quando o teclado está aberto.
- Remove o arquivo `commit_message` do versionamento.